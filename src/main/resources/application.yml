spring:
  ai:
    dashscope:
      api-key: ${AI_DASHSCOPE_API_KEY}
      chat:
        options:
          model: qwen-max
      embedding:
        options:
          model: text-embedding-v3

server:
  port: 8080

app:
  vectorstore:
    persistence-path: ./data/vector-store.json
  reranker:
    top-k: 3
    retrieve-top-k: 10


