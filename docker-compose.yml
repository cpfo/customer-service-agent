version: "3.8"

services:
  redis-stack:
    image: redis/redis-stack-server:latest
    container_name: customer-service-redis
    ports:
      - "6379:6379"
    volumes:
      - redis-data:/data
    restart: unless-stopped

volumes:
  redis-data:
